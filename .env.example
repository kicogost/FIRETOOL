# Phase 1: not used yet. The FIRE engine and debug page run on in-memory seed data.
# These variables are documented here for Phase 2+ (Supabase Postgres + Drizzle).

# Supabase Postgres connection string (used by drizzle-kit and the runtime client).
DATABASE_URL=

# Supabase project (for the client SDK / RLS in later phases).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
