/**
 * v1 is single-user (PRD §1): exactly one profile row, keyed to this fixed id.
 * Multi-user (Phase 6) replaces this constant with the authenticated user's id.
 */
export const SINGLE_PROFILE_ID = "00000000-0000-0000-0000-000000000001";
